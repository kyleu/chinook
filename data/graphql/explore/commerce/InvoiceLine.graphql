# Generated File
# Queries the system for Invoice Lines.
query InvoiceLine {
  invoiceLine(
    q: null, # Or string literal
    filters: null, # Or filters of type `{ k: "", o: Equal, v: "" }`
    orderBy: null, # Or orderBy of type `{ col: "", dir: Ascending }`
    limit: null, # Or number
    offset: null # Or number
  ) {
    totalCount
    paging {
      current
      next
      itemsPerPage
    }
    results {
      invoiceLineId
      invoiceId
      trackId
      unitPrice
      quantity
    }
    durationMs
    occurred
  }
}
